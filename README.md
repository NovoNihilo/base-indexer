# Base Indexer

Forward-only blockchain indexer for Base (OP Stack).

## Setup
```bash
npm install
cp .env.example .env
# Edit .env with your RPC URL
```

## Run
```bash
# Start indexing
npm run ingest

# View stats (in another terminal)
npm run stats
```
